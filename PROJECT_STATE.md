# RocketDict project state — L1

- Repo `oss-lk/gerkonkv-site`, branch `chatgpt/product-core-forward`. Current engineering checkpoint is `0d11d288b18fd562ad26d8c642d3711280920681` (`Test combined Greek source-plan rescue`). `rocketdict/PRODUCT_TARGET.md` remains authoritative; release still requires zero numeric/symbol, punctuation and length hard failures on the complete 90k+ corpus, then learner/export completeness and Windows clean-install/release validation.
- **Run59 remains the sole legal forward parent**: 3335 rows; hard counts **15 numeric / 10 punctuation / 0 length, 24 unique**. Canonical workflow `34753122528`, artifact `10316657472`, SQLite SHA `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`, output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`.
- Seq3009 complementary-question work did not justify promotion. OPUS clause-first DOE `34756122766` restored the missing question but failed strict research only because it introduced two quote delimiters around `Butter of Antimony`. The follow-up TC-big workflow `34756713385` failed **before inference** because the isolated runtime omitted `transformers`/`sacremoses`; this is orchestration-negative evidence, not model-negative evidence. Do not weaken quote/artifact gates.
- The active quality frontier is now the run59 combined-Greek residual seq1753/source start `301051`. Corpus census finds exactly two combined-Greek rows: seq282 already preserves `_nvt[Greek: t]_`; seq1753 loses `_A[Greek: a]_`. Generic trigger therefore isolates only seq1753 without sequence/source-start whitelist.
- Read-only combined-Greek source-plan DOE workflow `34757359712` succeeded. Artifact `10317379028`, digest `sha256:edca7a983d859c3b25f3caa4678c7fc297eab6b2a16e8153d06ab1d9b76a4b97`, evidence SHA `29ae03c4c5609ca698a578ca7b876e5111fd4d799fe74f1a7584099097e93b73`. The pre-MT source plan keeps `_A[Greek: a]_` and surrounding separators byte-exact/source-owned, translates only prefix/suffix with raw OPUS rank0, and produces `Тогда разделите линию _A[Greek: a]_ в таких долях, как цифры 1, 2, 3, 5, 6, 7, 9, 10, 11, &c., установленные в точках деления.`. Aggregate candidate passes all hard gates, strict research, emphasis, critical-token preservation and non-decreasing alphabetic volume; database remained unchanged; DOE itself grants no promotion authority.
- Default-OFF production wrapper `rocketdict-stage12-combined-greek-source-plan-rescue/1` was added in commit `54c52f97739a0e16907c7c19176419c5be899565` outside the run59 M2M100 arithmetic layer. It source-plans exactly one combined Greek technical atom before inference, copies the atom/separators from immutable source, permits only raw OPUS rank0 for lexical cells, and rejects back to the exact base target. Maintained Product Core workflow `34757474185` was fully green on the stage-only commit.
- Current HEAD adds neutral tests but Product Core workflow `34757502767` has **415 passed / 1 failed** in dependency-light tests while real-runtime succeeds. The single failure is a test-fixture bug: mocked `_complete` returns the truthy `items` list via `setdefault(...) or output`; production logic/gates are not implicated. Fix only the fixture and rerun maintained CI before any corpus promotion.
- Cleanup debt: `.github/workflows/rocketdict-full-opticks-tc-big-ct2-parity-run9.yml` is still repurposed for the failed seq3009 TC-big DOE and must eventually be restored exactly to historical blob `f677a98b18e18a9d3bf5bcbf88e1a9d1c4ebaf29`. Historical Run20 M2M100 workflow is already restored exactly.

## Next actions

1. Fix only the combined-Greek unit-test `_complete` fixture; require maintained dependency-light and real-runtime CI green on unchanged production logic.
2. If CI is green, run an authenticated run59 combined-Greek wrapper replay through the real public Stage12 path. Require exact run59 ancestry, one generic attempt/accept, only seq1753 target change, byte-exact source coverage, raw-rank0 lexical provenance, SQLite integrity/FKs and all safety flags false.
3. Run an independent residual census. Expected improvement is punctuation/critical-symbol only, but do not assume counts until evidence is persisted; replace run59 only after a separate canonical verifier grants parent replacement.
4. Restore repurposed TC-big parity workflow after the closed seq3009 evidence is durably retained.
5. Continue residual-family work until hard counts reach zero; then complete learner/export and Windows release validation.

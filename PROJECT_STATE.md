# RocketDict project state — L1

- Repo: `oss-lk/gerkonkv-site`; branch: `chatgpt/product-core-forward`. Engineering HEAD before this L1 catch-up: `29882910fc3bc2c54c6918d7362da47802e7a22e` (`Add canonical run59 promotion verifier`). Compared with the prior L1/L2 decision checkpoint `d06a8dd8833c9d4fa259f10a3bb2aaedc6762c31`, HEAD is 9 commits ahead. `rocketdict/PRODUCT_TARGET.md` remains authoritative: release requires zero unresolved numeric/symbol, punctuation and length hard failures on the complete 90k+ corpus, then learner/export completeness and Windows clean-install/release validation.
- **Run58 remains the sole legal parent until canonical run59 verification completes.** Run58: 3335 rows; **16 numeric / 10 punctuation / 0 length, 25 unique**; output SHA `60349256eac118e9af7aa1366761342cd2041bb6ade4db04b45c78b46a8c6e5d`; target SHA `8297e8b6cf20233284042549132bfc2eb4be312046964eab22cd7aa785eee3fd`; DB SHA `a24f35d6f8bb4747a4fc301511f383b8b866846a3c448655fce1b9359cc0ac94`; parameters SHA `1cf60c7b582640c3923ea685ed329eb685a7b80e502e11507d99c752e6342315`. Promotion workflow `34748268143`, artifact `10315026413`.
- M2M100 runtime provenance is now hardened without changing selector or generation semantics. CTranslate2 is pinned exactly to `4.8.2` in Product dependencies and runtime availability; runtime/cache identity includes CT2 version. Arithmetic rescue contract is `/2`, selected phase `v2`; rejected evidence retains raw rank0 target/tokens, score, generation and runtime identity. Full regressions after the patch: focused 15/15, Product Core 417/417, Workbench 213/213 + 1 skip; maintained Product Core dependency-light and real-runtime Stage8→25/unified Product run are green.
- Standalone reproducibility boundary is closed: exact arithmetic source start `483234` produces target SHA `258bc3a57d933b23317cf946af2f9e851fd266c6501a5b51813806b3294081d6` and raw-token SHA `599b77feba47448aff917fee3ea7d5e6e4a2c607378af906eaa63ca3ff37373d` with selector PASS across Linux AMD, Linux Intel and two Windows Server 2025 hosts, ordinary `384/512` and the tested forced-MKL CNR policy. Converted CT2 asset bytes are host/platform-sensitive, but this raw inference is stable; no evidence justifies changing beam/rank0/maxlen or forcing CNR.
- **Three independent public-Stage12 run58 replays are now green.** Workflow `34752772395` ran the actual `rocketdict.api.operations.run_stage12` chain three times from authenticated run58 with always-upload forensics. All three persisted run59, each `attempt=1`, `accepted=1`, `rejected=0`, only target sequence `2739` changed, exact source coverage/SQLite integrity/safety flags passed, and all produced the same promoted output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`, raw target SHA `258bc3…`, raw token SHA `599b77…`, CT2 `4.8.2`, beam 6 / rank0 / maxlen 512. Replica evidence SHAs: `fbaf00cff04908f196e12340e091466c4194a04adb90105c18b369184429c472`, `8f289ae562f5d1f649537e99f81e9ad24f103c3dc59e02a2937518bfaa2a97af`, `13fa0fcbd4cae46d50a0108a3ef24a9d04fba634ec6c46baf74cd54cc774764c`. Their SQLite file SHAs differ as expected from persisted DB metadata/layout, but semantic output is identical.
- Replay aggregate succeeded and authorizes only the next canonical-verification step, **not parent replacement**: evidence SHA `3203f6dd27be33e0812d147e13e8379e1a24c95a3b622db9a774a2599e4f4a37`; artifact `10316012582`, digest `sha256:2ba0aca1e69b2172b8ad42e2b247be3bb471c4a3cc37123f4a6f9f6d07ce1fe0`; `canonical_promotion_authorized=true`, `parent_replacement_allowed=false`. Replica artifacts: `10316417841`, `10316357926`, `10316567512`.
- A no-inference canonical verifier now exists at `rocketdict-workbench/tests/verify_run59_m2m100_arithmetic_canonical_promotion.py`. It requires replay aggregate authority, one persisted replica DB, independent residual census, exact run58 ancestry, run59 id, 3335 rows, only seq2739 changed, raw rank0 target/token identities, contract `/2`, CT2 4.8.2/generation identity, byte-exact source coverage, all safety flags false, SQLite integrity, and expected hard counts **15 numeric / 10 punctuation / 0 length, 24 unique** before setting `parent_replacement_allowed=true`.
- Historical reject `34749776816` remains unrecoverable at raw-output level only because orchestration raised before artifact upload; Stage12 itself already retained rejected target provenance. New replay orchestration fixes this with unconditional forensic upload.
- Process cleanup remains pending until canonical run59 promotion/census is durable: remove one-shot reproducibility/public-replay workflows/scripts and restore `.github/workflows/rocketdict-full-opticks-run20-m2m100-product-restatement-doe.yml` exactly from historical commit `6f5c8164f1d556aea55be6193cc3e864d9e0e09e`.
- Next bounded quality direction after arithmetic closure: source-defined internal leading-question paragraph-boundary truncation at run58 seq3009/source start `531604`; historical exact-row TC-big raw rank0 passed hard punctuation/emphasis/volume. Keep separate until run59 is legal parent.

## Next actions

1. Canonicalize one already-persisted replay run59 **without another model call**: download replay aggregate + replica1 DB/evidence, run independent residual census, then `verify_run59_m2m100_arithmetic_canonical_promotion.py`.
2. If canonical verifier is green, promote run59 as sole legal parent and record exact canonical DB/output/parameters/target/evidence/census hashes in L1/L2.
3. Restore the historical M2M100 DOE workflow and remove temporary determinism/CNR/asset/Windows/public-replay/canonical-verifier infrastructure after evidence is durably retained.
4. Continue quality-first from run59 residuals, beginning with the bounded seq3009 question-boundary TC-big family; require separate default-OFF implementation, full-corpus promotion and independent census.
5. After zero hard failures, finish learner/export completeness and Windows clean-install/release validation.

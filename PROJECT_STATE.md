# RocketDict project state — L1

- Branch: `chatgpt/product-core-forward`. Current engineering checkpoint is `8902a3e169573618a516be0cbbea0c80a0872747`. `rocketdict/PRODUCT_TARGET.md` remains authoritative; release still requires zero numeric/symbol, punctuation and length hard failures on the full 90k+ corpus.
- **Run59 remains the sole legal forward parent**: 3335 rows; hard counts **15 numeric / 10 punctuation / 0 length, 24 unique**. Canonical workflow `34753122528`, artifact `10316657472`, SQLite SHA `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`, output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`.
- M2M100 arithmetic rescue remains accepted default-OFF and cross-platform reproducible. Historical Run20 workflow has been restored to historical blob `a12988246a6306d3c6c9d0afdbf8f51bbbc0d983`; Windows repro and run58 public-replay workflow cleanup remains non-blocking debt.
- Current quality frontier is punctuation seq3009 inside Stage10 context 2730. Canonical source geometry spans run59 rows 3005..3009; row3009 begins with `fix'd as Lead?` followed by a blank paragraph and a long new paragraph. The maintained split loses that leading question.
- Simple two-row forward-boundary DOE v2 (`34755358217`) authenticated canonical run59 and OPUS but produced trigger cohort 0 because its routing predicate required row3008 to be strict-research clean. L3 confirms row3008 is hard-clean but its existing target introduces two quote delimiters around `Butter of Antimony`, so that base-only research gate is too restrictive for routing; candidate outputs must still remain fully strict-clean.
- A broader source-defined complementary-question clause DOE was then implemented and run successfully: workflow `34756122766`, artifact `10317896538`, digest `sha256:7aaec9e01bf76a6e67a90dfab17bd104be4cfa7303771e58af6c42b806ea2dc0`, evidence SHA `8d959993fe5872d2c83e9a5404052724aa01eb95bbc9ac3c576f1de33e3266d8`. It discovers exactly context2730 from source/failure geometry without sequence/start whitelists, resegments the complete context into 7 clause-first raw OPUS rank0 requests with token counts `[42,52,30,38,54,44,56]`, restores the missing question and yields an aggregate that passes all Product hard gates and emphasis/volume checks. Promotion remains blocked because OPUS introduces two target-only quote delimiters around `Butter of Antimony`; aggregate strict-research therefore fails.
- HEAD adds a read-only pinned TC-big DOE over the exact same source-defined clause geometry (`real_translation_run59_tc_big_complementary_question_clause_doe.py`), but no TC-big workflow/result has yet been recorded. This is the next bounded experiment; selectors/gates must remain unchanged.

## Next actions

1. Run the prepared TC-big complementary-question clause DOE on canonical run59 with raw rank0 only and unchanged strict selectors.
2. If TC-big removes the quote artifact while keeping all hard/research/emphasis/volume checks and semantics sound, implement a separate default-OFF wrapper and require full run59 promotion plus independent census before parent replacement. Otherwise close this geometry without weakening gates.
3. Continue residual-family work to zero hard failures; keep cleanup secondary unless it affects correctness/reproducibility.

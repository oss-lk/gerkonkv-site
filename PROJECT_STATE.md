# RocketDict project state — L1

- Repo: `oss-lk/gerkonkv-site`; branch: `chatgpt/product-core-forward`; recovery checkpoint is `005b9163a3758e6522a9660ac09f39920889f752` before this memory catch-up commit. `rocketdict/PRODUCT_TARGET.md` remains authoritative: release requires zero unresolved numeric/symbol, punctuation and length hard failures on the complete 90k+ corpus, then complete learner/export coverage and Windows clean-install validation.
- **Authenticated run58 remains the sole legal forward parent.** Promotion workflow `34748268143`, artifact `10315026413` (`sha256:265ae3de4991544990aedf752451feb84733a3237ca3ae2bac2dfd49e97befc9`). Run58: 3335 rows; **16 numeric / 10 punctuation / 0 length, 25 unique**; translation output SHA `60349256eac118e9af7aa1366761342cd2041bb6ade4db04b45c78b46a8c6e5d`; target SHA `8297e8b6cf20233284042549132bfc2eb4be312046964eab22cd7aa785eee3fd`; SQLite SHA `a24f35d6f8bb4747a4fc301511f383b8b866846a3c448655fce1b9359cc0ac94`; parameters SHA `1cf60c7b582640c3923ea685ed329eb685a7b80e502e11507d99c752e6342315`.
- Run58 promotion evidence SHA `0d5e893f5338e3b40c82a0e6eeb91d6f6b56dfc15fbae5974f0f6e9f042bb8c2`; independent residual-census SHA `f4db643126c611e5f41e07af9d2ffa63dc43e9e9e48ea7884b0da7563ef9a803`; SQLite `integrity_check=ok`, FK violations 0.
- Pinned optional M2M100 portability layer is implemented: `facebook/m2m100_418M`, revision `55c2e61bbf05dfb8d7abccdc3fae6fc8512fd636`, weight SHA `d907ea45e4e4b9db163382a6674f6218b3c59566fe06d77f4055c208b4e87ed1`; exact snapshot -> Product CTranslate2 float32 asset -> CPU torch-free inference. It is not a Product-default replacement for OPUS.
- Real parity workflow `34748655508`, artifact `10315206479` (`sha256:3f90caf7dcf13a807da14a5d3ebb65100ee7ed9c6478d43b8853ff64d4912a1e`) proved raw CT2 rank0 for source start `483234` byte-identical to historical PyTorch rank0 under the parity generation contract (`beam=6`, `num_hypotheses=1`, `max_decoding_length=384`), preserving verified `700000 × 700000 = 490000000000`; evidence SHA `bc739df0594e5f73331c2a2d5301bdb6d6eb27d9ab3921703dc99a16237dac57`.
- Generic source-defined arithmetic trigger/selector and a default-OFF Stage12 M2M100 arithmetic wrapper are implemented. Synthetic fixture defects were fixed without weakening gates; maintained Product CI `34749438950`, public outer-wrapper binding CI `34749689194`, and diagnostic-head CI `34750071691` all passed dependency-light + real Stage8→25/unified runtime.
- Public `rocketdict.api.operations.run_stage12` now binds to the outer M2M100 arithmetic wrapper while preserving default-OFF behavior; no M2M100 inference occurs without explicit opt-in.
- Full run58-based arithmetic promotion is **not yet accepted**. Workflow `34749776816` authenticated exact run58/TC-big/M2M100 assets and exact base ancestry, found exactly one attempt at source start `483234`, but persisted `accepted=0/rejected=1`; no run59 was authorized. Diagnostic replay `34750071679` on the same production code and same authenticated run58 instead produced `attempt=1, accepted=1, rejected=0` and exact base run58 identity, then intentionally failed. This run-to-run disagreement is the active blocker: determine whether raw CT2 rank0 or a runtime-dependent verdict is nondeterministic before any promotion. Do not weaken selector/hard/emphasis/notation gates.
- Production arithmetic wrapper currently uses `max_decoding_length=512`, while the successful parity evidence used `384`. This is a suspect generation-contract drift, not yet a proven cause of the run-to-run disagreement.
- Punctuation follow-up candidate: source-defined internal leading-question paragraph-boundary truncation (`?\n\n` inside a row, short prefix, long suffix, source one `?`, target zero) isolates run58 seq3009/source start `531604`; historical exact-row TC-big rank0 passed hard punctuation/emphasis/volume. Do not implement until arithmetic promotion/reproducibility is resolved.
- Historical M2M100 compound-million output `десятьсот тысяч` remains semantically rejected despite a green numeric gate. Source start `2995`/critical-emphasis direction is also closed for M2M100 because historical rank0 loses `_per deliquium_`.
- Process cleanup debt remains: `.github/workflows/rocketdict-full-opticks-run20-m2m100-product-restatement-doe.yml` is temporarily repurposed as the run58 arithmetic promotion/diagnostic runner, and `rocketdict-workbench/tests/real_translation_full_opticks_m2m100_arithmetic_promotion.py` is one-shot research infrastructure. Restore the historical workflow blob and delete the one-shot harness after the arithmetic conclusion/evidence is retained.

## Next actions

1. Reproduce/diagnose the `34749776816` reject vs `34750071679` accept disagreement using exact raw rank0 and runtime/generation identities. Treat reproducibility as a quality requirement; do not promote a stochastic/unstable path.
2. If the only difference is generation-contract drift, align the production wrapper with the already-proven parity contract and add regression coverage; otherwise fix the actual deterministic defect without weakening gates.
3. Repeat authenticated full run58-based promotion plus independent census. Only a reproducible green corpus replay may create run59 and replace run58 as parent.
4. Restore the historical M2M100 workflow and remove one-shot arithmetic research infrastructure after the result is durably recorded.
5. Continue quality-first toward zero hard failures, then learner/export completeness and Windows release validation.

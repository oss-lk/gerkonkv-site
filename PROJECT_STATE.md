# RocketDict project state — L1

- Branch: `chatgpt/product-core-forward`. Current engineering checkpoint is `167424b592b783337dcdf8fc367dea799f64893d`; this commit temporarily repurposes the historical TC-big CT2 parity workflow as a read-only runner for the already-prepared run59 complementary-question clause DOE. After durable evidence is captured, restore that workflow exactly to historical blob `f677a98b18e18a9d3bf5bcbf88e1a9d1c4ebaf29`. `rocketdict/PRODUCT_TARGET.md` remains authoritative; release still requires zero numeric/symbol, punctuation and length hard failures on the full 90k+ corpus.
- **Run59 remains the sole legal forward parent**: 3335 rows; hard counts **15 numeric / 10 punctuation / 0 length, 24 unique**. Canonical workflow `34753122528`, artifact `10316657472`, SQLite SHA `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`, output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`.
- M2M100 arithmetic rescue remains accepted default-OFF and cross-platform reproducible. Historical Run20 workflow is restored exactly to blob `a12988246a6306d3c6c9d0afdbf8f51bbbc0d983`; Windows repro and run58 public-replay workflow cleanup remains non-blocking debt.
- Current quality frontier is punctuation seq3009 inside Stage10 context2730. Canonical source geometry spans run59 rows 3005..3009; row3009 begins with `fix'd as Lead?` followed by a blank paragraph and long new material. The maintained split loses that leading question.
- Simple two-row forward-boundary DOE v2 (`34755358217`) found cohort 0 because routing required row3008 to be strict-research clean. Row3008 is Product-hard clean but its existing target adds two quote delimiters around `Butter of Antimony`; candidate outputs, not dirty base rows, must remain fully strict-clean.
- Source-defined complementary-question clause OPUS DOE is the current geometry authority: workflow `34756122766`, artifact `10317896538`, digest `sha256:7aaec9e01bf76a6e67a90dfab17bd104be4cfa7303771e58af6c42b806ea2dc0`, evidence SHA `8d959993fe5872d2c83e9a5404052724aa01eb95bbc9ac3c576f1de33e3266d8`. It discovers exactly context2730 without sequence/start/lexical whitelists, partitions immutable source into 7 clause-first raw rank0 inputs with token counts `[42,52,30,38,54,44,56]`, restores the missing question and passes all Product hard gates plus emphasis/volume. Promotion is blocked because OPUS introduces two target-only quote delimiters around `Butter of Antimony`, so aggregate strict-research fails.
- A read-only pinned TC-big DOE over **the exact same generic geometry and unchanged selector** is now launched by checkpoint `167424b…`. It uses pinned `Helsinki-NLP/opus-mt-tc-big-en-zle` revision `708be1d372fe4c358a352f404e6dc9ca0126ba48`, verified weight SHA `e68caa9a233c177a3489257b69c18cece6da97767ab2581918ce3fc3c3899416`, CT2 float32 and a torch-free inference venv. The runner never grants promotion authority and persists negative evidence as well as positive evidence.

## Next actions

1. Inspect the TC-big complementary-question DOE result/artifact from the `167424b…` runner. If strict or semantic review fails, close this family without weakening gates.
2. If TC-big is strict-clean and semantically acceptable, implement a separate default-OFF wrapper using the same generic discovery/segmentation and raw-rank0-only selector; then require authenticated run59 promotion and an independent census before parent replacement.
3. Restore the historical TC-big parity workflow exactly to blob `f677a98b18e18a9d3bf5bcbf88e1a9d1c4ebaf29` after DOE evidence is durable.
4. Continue residual-family work until all hard counts are zero; keep non-blocking cleanup secondary unless it affects correctness or reproducibility.

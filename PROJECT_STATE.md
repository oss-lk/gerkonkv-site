# RocketDict project state — L1

- Repo: `oss-lk/gerkonkv-site`; branch: `chatgpt/product-core-forward`. Last engineering/research HEAD before this memory catch-up is `f4f63ec314186d4eccb2f55f67c0f3665fddf566`; mandatory L2 catch-up commits immediately before this L1 update are `bd027189a92d7bb562631649cd32e0c59b47d865` and `83c64787ead00dd20fa8f9946ac5e427f660d8f3`. `rocketdict/PRODUCT_TARGET.md` remains authoritative: release requires zero unresolved numeric/symbol, punctuation and length hard failures on the complete 90k+ corpus, then complete learner/export coverage and Windows clean-install validation.
- **Authenticated run58 remains the sole legal forward parent.** Promotion workflow `34748268143`, artifact `10315026413` (`sha256:265ae3de4991544990aedf752451feb84733a3237ca3ae2bac2dfd49e97befc9`). Run58: 3335 rows; **16 numeric / 10 punctuation / 0 length, 25 unique**; translation output SHA `60349256eac118e9af7aa1366761342cd2041bb6ade4db04b45c78b46a8c6e5d`; target SHA `8297e8b6cf20233284042549132bfc2eb4be312046964eab22cd7aa785eee3fd`; SQLite SHA `a24f35d6f8bb4747a4fc301511f383b8b866846a3c448655fce1b9359cc0ac94`; parameters SHA `1cf60c7b582640c3923ea685ed329eb685a7b80e502e11507d99c752e6342315`. Promotion evidence SHA `0d5e893f5338e3b40c82a0e6eeb91d6f6b56dfc15fbae5974f0f6e9f042bb8c2`; independent census SHA `f4db643126c611e5f41e07af9d2ffa63dc43e9e9e48ea7884b0da7563ef9a803`; SQLite integrity/FKs clean.
- Pinned optional M2M100 layer remains default-OFF: `facebook/m2m100_418M`, revision `55c2e61bbf05dfb8d7abccdc3fae6fc8512fd636`, source-weight SHA `d907ea45e4e4b9db163382a6674f6218b3c59566fe06d77f4055c208b4e87ed1`, float32 CTranslate2 CPU torch-free inference. Public Stage12 reaches the outer arithmetic wrapper only when explicitly enabled.
- Parity workflow `34748655508` proved exact source start `483234` raw CT2 rank0 equals historical PyTorch rank0 under `beam=6`, `num_hypotheses=1`, `max_decoding_length=384`, preserving verified `700000 × 700000 = 490000000000`; evidence SHA `bc739df0594e5f73331c2a2d5301bdb6d6eb27d9ab3921703dc99a16237dac57`.
- Full run58 arithmetic promotion is **not accepted yet**. Replay `34749776816` isolated exactly one arithmetic attempt at source start `483234` but returned `accepted=0/rejected=1`; diagnostic replay `34750071679` on the same production code/base returned `attempt=1, accepted=1, rejected=0`. The rejected raw rank0 was not persisted, so that one historical disagreement cannot be forensically reconstructed. Do not weaken selector/hard/emphasis/notation gates.
- New reproducibility evidence substantially narrows the blocker. Broad CTranslate2 `4.8.2` workflow `34750592651` repeated the exact source across two fresh processes × four repeats for current-auto/single-thread, 384/512 and portable-DNNL configurations. AMD EPYC and Intel Xeon hosts all produced the same raw target SHA `258bc3a57d933b23317cf946af2f9e851fd266c6501a5b51813806b3294081d6`, exact parity, and selector PASS. The Intel job's post-verifier failed only because the research verifier incorrectly assumed one universal CT2 asset-tree SHA; inference itself was green. Therefore `512` vs `384` is no longer a supported explanation for the rejected promotion.
- Focused forced-MKL CNR workflow `34751000550` is fully green. Three AMD generations, both `384` and `512`, fresh processes, `CT2_USE_MKL=1` + `MKL_CBWR=COMPATIBLE` + one thread/dynamic-off/AVX2/packed-GEMM-off all produced the same exact-parity raw rank0 and selector PASS; cross-host CNR evidence SHA `a4d8a4d0…`. This is a proven fallback policy, not yet Product default.
- Asset-build reproducibility workflow `34751186058` proved three AMD hosts build the same CT2 payload byte-for-byte, tree `09d8b2e6b5941382aa7c695d2fbbf8918244a8df62ca9befc72d89e43f4642eb`, cross-host evidence SHA `b0ccd7758590a7761eb9a46b33bfdeb677524ac39b019431cf492152787b844b`. Intel conversion produced a different tree while inference still matched. Therefore CT2 converted bytes are platform/host-class sensitive and source-checkpoint identity must not be treated as a universal converted-asset hash.
- Windows reproducibility workflow `34751346680` is active. Replica 2 on Windows Server 2025 completed successfully: Windows-built asset tree `3f3539411fb3eed88ebbf4c42b73f0f044b3b33e9182f46e0f897c57b195d7f5`; all current/single/portable 384/512 cases and forced-MKL-CNR 384/512 cases produced exact parity target SHA `258bc3…` and selector PASS; Windows evidence SHA `39e83e5bf846a2b72cbb4cd698ecb7513fc41ca9151232194ac4440f1da55252`. Replica 1 had completed ordinary current/portable inference successfully and was still running its CNR matrix at this checkpoint. Do not claim cross-Windows completion until L3 confirms it.
- Upstream CTranslate2 behavior relevant to release hardening is now established: prebuilt x86-64 binaries auto-select backend/ISA by CPU; CPU numerical differences can affect beam results; backend choice is cached process-statically after first use. If a forced backend policy is ever required, establish it before CTranslate2 initialization or isolate it in a subprocess; do not mutate backend env after OPUS has initialized the runtime.
- Evidence-forensics requirement: every future promotion/rejection must retain raw rank0 target/tokens, CTranslate2 version, generation parameters/execution policy, asset manifest/tree identity and host/platform class. Fail-closed rejection without this provenance is not enough evidence to change generation contracts.
- Punctuation follow-up candidate remains source-defined internal leading-question paragraph-boundary truncation (`?\n\n` inside a row, short prefix, long suffix, source one `?`, target zero), isolating run58 seq3009/source start `531604`; historical exact-row TC-big rank0 passed hard punctuation/emphasis/volume. Keep separate until arithmetic promotion/reproducibility is closed.
- Historical M2M100 compound-million `десятьсот тысяч` and `_per deliquium_` direction remain closed. Process cleanup debt remains: temporary determinism/CNR/asset/Windows workflows and the repurposed historical M2M100 workflow must be removed/restored after conclusions and final promotion evidence are retained.

## Next actions

1. Finish/authenticate Windows replica 1 and compare both Windows replicas; determine whether Windows asset tree and raw inference are reproducible across independent hosted Windows machines.
2. Harden M2M100 runtime/promotion provenance without changing translation semantics: pin/record the CTranslate2 version and persist raw rejected rank0 plus execution/asset/host identity.
3. Re-run authenticated run58 arithmetic promotion multiple times through the public Stage12 chain. Require the same raw rank0 and selector verdict across independent runs; if stable, complete run59 persistence plus independent residual census before replacing run58.
4. Restore the historical M2M100 workflow and remove one-shot reproducibility/promotion harnesses once evidence is durable.
5. Continue quality-first from the remaining run58/run59 residuals toward zero hard failures, then learner/export completeness and Windows release validation.

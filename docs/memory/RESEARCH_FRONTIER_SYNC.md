# RocketDict research frontier synchronization note

Purpose: temporary synchronization anchor while L1/L2 documents are being refreshed against the actual engineering HEAD.

Current conclusions:

- MetricX QE is an evaluation component, not a development blocker.
- Clause-boundary feasibility research already exists in `.github/workflows/rocketdict-full-opticks-clause-boundary-feasibility.yml` and `rocketdict-workbench/tests/real_translation_full_opticks_clause_boundary_feasibility.py`.
- The active quality frontier is comparison of existing research branches against immutable full-corpus evidence, not reimplementation of already completed experiments.
- Any future Product promotion requires regression evidence and preservation of immutable source coverage.

Required next memory maintenance:
- refresh `PROJECT_STATE.md` from current HEAD;
- update `docs/memory/TRANSLATION_QUALITY.md` with current experiment matrix;
- update `docs/memory/DECISIONS.md` only with durable conclusions, not chronology.

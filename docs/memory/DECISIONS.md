# RocketDict durable decisions — L2

- Quality gates remain authoritative; a mechanically green candidate is insufficient when semantic review finds substantive loss.
- **Authenticated run57 is the sole legal forward parent.** Promotion `34746052346` proved exact run56 ancestry, four intended row replacements, unchanged unrelated targets, exact-source/raw-rank0 provenance, database integrity and independent counts **18 numeric / 10 punctuation / 0 length, 27 unique**.
- Wrapper layering is part of run identity. Run56 is the emphasized-modifier wrapper over run55 illustration `/5`; therefore the row-local parenthetical wrapper must delegate through `translation_emphasized_modifier_boundary_rescue_stage`. The illustration-direct change was rejected by L3 ancestry evidence and reverted before run57 promotion.
- Whole-context parenthetical replacement is not accepted because context `1977` semantically truncates despite mechanical eligibility. The approved tested geometry is row-local, default-OFF, exact immutable source -> pinned TC-big unique raw rank0, fail-closed.
- Promotion evidence SHA `8565e338f6ed8993498427d67235fa03062728c02edc0e19a4cd3826a8122e54`; run57 census SHA `38192e8ea390637012cdb2ecb4aeea0eae76a72a3edc17bf3c2df6004d4109a7`.
- Previously closed residual geometries remain closed unless materially new evidence appears.
- Forward work must start from run57 census. Exact-row numeric model screening may be used only as read-only discovery evidence; any production rescue still requires a generic source-defined trigger, semantic review, neutral tests and independent full-corpus promotion.

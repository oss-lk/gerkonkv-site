# RocketDict durable decisions — L2

- Quality gates are authoritative. Mechanically green output is insufficient when semantics, emphasis, notation, provenance or reproducibility fail. Release promotion requires evidence from the real Product path and the full acceptance corpus.
- **Run59 remains the sole legal forward parent.** Canonical workflow `34753122528`, artifact `10316657472`, 3335 translation rows, only seq2739 changed from run58, exact source coverage, SQLite integrity/FKs and safety flags verified. Canonical DB SHA `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`; output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`. Current hard counts: **15 numeric / 10 punctuation / 0 length, 24 unique**.
- M2M100 arithmetic restatement is accepted **default-OFF**, not a replacement for the OPUS baseline. The pinned runtime remains `facebook/m2m100_418M` revision `55c2e61bbf05dfb8d7abccdc3fae6fc8512fd636`, source-weight SHA `d907ea45e4e4b9db163382a6674f6218b3c59566fe06d77f4055c208b4e87ed1`, CTranslate2 `4.8.2`, CPU float32, raw rank0 only. Cross-platform evidence does not justify maxlen 512→384 or forcing MKL CNR.
- Any promotion/diagnostic workflow must preserve and upload rejected raw target/tokens and runtime/asset/generation evidence before failing. A runner/orchestration identity failure is not model-negative evidence.
- For seq3009, prefer fixing source segmentation geometry before adding another MT fallback. L3 shows the row starts with a short trailing question fragment `fix'd as Lead?`, followed by a blank paragraph break and a long new paragraph; planner-v8 places that fragment at the next row start after filling the preceding row to the 64-token soft budget. Historical exact-row TC-big restores punctuation but leaves untranslated `fix'd`, so it is rejected as insufficient semantic quality.
- The active experiment is a **generic forward paragraph-boundary resegmentation** with baseline OPUS: move only a bounded short `...? + blank paragraph` prefix from the right split row to the left split row, keeping immutable source coverage exact. No sequence/source-start whitelist may define eligibility. Candidate persistence, if later implemented, must remain raw rank0 only and fail closed on every existing hard/research/emphasis/volume check.
- Initial boundary DOE workflow `34755006902` failed before model inference because the harness used a stale run59 SQLite SHA. The canonical SHA is `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`. Corrected launcher `real_translation_run59_forward_paragraph_boundary_resegmentation_doe_v2.py` repairs only this identity. This failure must not be counted as negative model evidence.
- If the corrected resegmentation DOE fails strict/semantic review, close the direction without weakening selectors. If it passes, a separate default-OFF production wrapper, full run59-based promotion replay and independent residual census are mandatory before any parent replacement.
- Previously closed negative directions remain closed unless materially new L3 evidence appears, including progression-duplicate preservation failures and historical M2M100 `_per deliquium_` loss.
- One-shot arithmetic/reproducibility/canonical workflows are disposable after conclusions are durably retained. Historical Run20 DOE workflow must remain historically faithful; exact-byte restoration from commit `6f5c8164f1d556aea55be6193cc3e864d9e0e09e` is still cleanup debt if current blob identity differs.

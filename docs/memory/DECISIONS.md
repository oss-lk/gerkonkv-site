# RocketDict durable decisions — L2

- Quality gates remain authoritative; mechanically green output is insufficient when semantic/emphasis/notation evidence fails. Release promotion additionally requires reproducible model/runtime behavior for the accepted path, not a one-off green corpus replay.
- **Authenticated run58 is the sole legal forward parent.** Workflow `34748268143` + artifact `10315026413` prove exact run57 ancestry, only rows `2288,2355` changed, 3333 unrelated targets exact, raw TC-big rank0/exact-source provenance, SQLite integrity and independent hard counts **16 numeric / 10 punctuation / 0 length, 25 unique**. Promotion evidence SHA `0d5e893f5338e3b40c82a0e6eeb91d6f6b56dfc15fbae5974f0f6e9f042bb8c2`; census SHA `f4db643126c611e5f41e07af9d2ffa63dc43e9e48ea7884b0da7563ef9a803`.
- TC-big numeric-row routing remains default-OFF and may select only generic source-defined exact-row candidates with unique raw rank0, current strict gates, emphasis preservation and variant notation checks. Progression duplicate stays fail-closed; do not relax `_viz._`/`&c.` preservation.
- A numeric gate PASS alone does not authorize model routing. Historical M2M100 for `ten hundred thousand` produces semantically poor `десятьсот тысяч`; keep it rejected even though numeric `/6` is satisfied.
- Pinned M2M100 is allowed as an optional research/rescue component only through immutable provisioning and offline torch-free inference: repository `facebook/m2m100_418M`, revision `55c2e61bbf05dfb8d7abccdc3fae6fc8512fd636`, weight SHA `d907ea45e4e4b9db163382a6674f6218b3c59566fe06d77f4055c208b4e87ed1`, CTranslate2 float32 CPU runtime. It is not a Product-default replacement for OPUS.
- Arithmetic restatement is a distinct source-verifiable rescue class. Real parity workflow `34748655508` + artifact `10315206479` proved the CT2 raw rank0 for source start `483234` byte-identical to historical PyTorch rank0 under generation contract `beam=6`, `num_hypotheses=1`, `max_decoding_length=384` and preserving verified `700000 × 700000 = 490000000000`; evidence SHA `bc739df0594e5f73331c2a2d5301bdb6d6eb27d9ab3921703dc99a16237dac57`.
- Generic arithmetic trigger/selector remains source-defined: exactly one `A x B (that is, above C)` restatement with integer `A*B=C`, no corpus-position whitelist, current target exhibiting the intended numeric corruption, and candidate preserving exact arithmetic order/operator plus current strict/emphasis/volume requirements. The default-OFF Stage12 wrapper must persist only the unique raw rank0 over exact immutable source and must leave the base target unchanged on rejection.
- Public `rocketdict.api.operations.run_stage12` is intentionally bound to the outer arithmetic wrapper so explicit research/product opt-ins are reachable through the normal API. This does **not** enable M2M100 by default; the default configuration delegates through the existing Stage12 chain unchanged. Maintained dependency-light and real Stage8→25/unified runtime CI are green after this binding (`34749689194`, `34750071691`).
- **Do not promote run59 until M2M100 inference is reproducible.** Full promotion replay `34749776816` and diagnostic replay `34750071679` used the same production rules/wrapper, authenticated exact run58 ancestry and pinned assets, and both isolated exactly source start `483234`; the former yielded `accepted=0/rejected=1`, the latter `accepted=1/rejected=0`. This is evidence of an unresolved run-to-run difference, not a reason to relax selector gates. Diagnose raw rank0/runtime determinism first.
- Production arithmetic wrapper currently uses `max_decoding_length=512` while the proven parity run used `384`. Treat that as a candidate root cause/generation-contract drift only after repeated exact inference establishes what changes. If correction is needed, prefer alignment to the previously proven contract rather than threshold relaxation or target surgery.
- The source-defined internal leading-question paragraph-boundary geometry (`?\n\n` inside one row, short leading question, long following paragraph, source one `?`, target zero) is a promising separate TC-big punctuation family; on run58 it isolates seq3009/source start `531604`. It must remain separate from arithmetic rescue and requires its own default-OFF wrapper/full-corpus promotion if pursued.
- Historical M2M100 for the `_per deliquium_` critical-emphasis residual is closed because rank0 drops the emphasized phrase; do not weaken emphasis preservation to admit it.
- One-shot arithmetic promotion/diagnostic infrastructure and the temporarily repurposed historical M2M100 workflow must be removed/restored after the arithmetic conclusion is durably retained. Historical workflow content is evidence and must not remain overwritten by temporary research orchestration.
- Previously closed residual geometries remain closed unless materially new evidence appears.

# RocketDict durable decisions — L2

- Quality gates are authoritative. Mechanically green output is insufficient when semantics, emphasis, notation, provenance or reproducibility fail. Release promotion requires evidence from the real Product path and full acceptance corpus.
- **Run59 remains the sole legal forward parent.** Canonical workflow `34753122528`, artifact `10316657472`, 3335 translation rows, exact source coverage and clean SQLite/FKs. Canonical DB SHA `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`; output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`; hard counts **15 numeric / 10 punctuation / 0 length, 24 unique**.
- Seq3009 complementary-question clause geometry is not currently promotable. OPUS fixes the missing question but violates output-artifact quote preservation; do not relax that gate. The TC-big follow-up run failed before inference because the isolated runtime lacked dependencies required by the existing runtime probe, so it neither accepts nor rejects the model direction.
- A combined Gutenberg Greek atom such as `_A[Greek: a]_` may be treated as a **source-owned technical atom** when a pre-MT source plan proves exact byte coverage and lexical material exists on both sides. Translating only the exact lexical prefix/suffix and rendering the technical atom plus separators from immutable source is structural rendering, not post-translation literal injection. The semantic carrier must still pass the complete maintained aggregate hard/research/emphasis/critical-token selector.
- Run59 combined-Greek DOE `34757359712` is the evidence for this class: exactly two corpus rows contain combined Greek notation, only seq1753 loses its atom, and the generic trigger selects exactly that row. Artifact `10317379028`, digest `edca7a983d859c3b25f3caa4678c7fc297eab6b2a16e8153d06ab1d9b76a4b97`, evidence SHA `29ae03c4c5609ca698a578ca7b876e5111fd4d799fe74f1a7584099097e93b73`. Source-plan OPUS rank0 candidate is strict-clean and semantically preserves `_A[Greek: a]_` plus the numeric sequence/`&c.`.
- The production implementation is a separate **default-OFF** wrapper `rocketdict-stage12-combined-greek-source-plan-rescue/1` above the run59 M2M100 arithmetic layer. It must not be promoted based on DOE alone. Required next authority is maintained CI, then authenticated run59 replay through the real Stage12 path, independent residual census, and a canonical verifier before parent replacement.
- The current HEAD CI failure is test-only: a fake `_complete` returns `items` because of Python `setdefault(... ) or output` truthiness. Fix the fixture only; do not alter production code, trigger or selector for this failure.
- Historical TC-big parity workflow remains temporarily repurposed from seq3009 research and should be restored to historical blob `f677a98b18e18a9d3bf5bcbf88e1a9d1c4ebaf29` after durable cleanup. Historical Run20 M2M100 workflow is already restored exactly.
- Promotion/diagnostic workflows must retain/upload rejection evidence before failing; orchestration/dependency failures are not model-negative evidence.

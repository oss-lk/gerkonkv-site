# RocketDict durable decisions — L2

- Quality gates remain authoritative; mechanically green output is insufficient when semantic/emphasis/notation evidence fails.
- **Authenticated run58 is the sole legal forward parent.** Workflow `34748268143` + artifact `10315026413` prove exact run57 ancestry, only rows `2288,2355` changed, 3333 unrelated targets exact, raw TC-big rank0/exact-source provenance, SQLite integrity and independent hard counts **16 numeric / 10 punctuation / 0 length, 25 unique**. Promotion evidence SHA `0d5e893f5338e3b40c82a0e6eeb91d6f6b56dfc15fbae5974f0f6e9f042bb8c2`; census SHA `f4db643126c611e5f41e07af9d2ffa63dc43e9e48ea7884b0da7563ef9a803`.
- TC-big numeric-row routing remains default-OFF and may select only generic source-defined exact-row candidates with unique raw rank0, current strict gates, emphasis preservation and variant notation checks. Promotion-safe current variants are dense `…A` formula missing-literal and apostrophe-decimal truncation.
- Progression duplicate is not promotable under current CT2 evidence: rank0 loses `_viz._` and rewrites `&c.`. Keep it as a fail-closed negative control rather than weakening selectors.
- A numeric gate PASS alone does not authorize model routing. Historical M2M100 for `ten hundred thousand` produces semantically poor `десятьсот тысяч`; keep this rejected even though numeric `/6` is satisfied.
- Arithmetic restatement is a separate promising class because the source itself supplies a checkable equation/restatement. Historical M2M100 rank0 for source start `483234` preserves `700000 × 700000 = 490,000,000,000`, passes maintained mechanical/emphasis checks, and is semantically plausible. Before any Product wrapper, require pinned offline M2M100 CTranslate2 conversion/runtime parity and a generic arithmetic-verification trigger; PyTorch research output alone is not promotion authority.
- One-shot evidence infrastructure should be removed after durable artifact persistence; historical reusable workflows must be restored rather than left repurposed. Cleanup commit `17a21f3f3665ea0c8695e83ca04d9a742f3aa3c3` restored the numeric-shadow workflow after run58.
- Previously closed residual geometries remain closed unless materially new evidence appears.

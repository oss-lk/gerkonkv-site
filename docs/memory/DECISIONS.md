# RocketDict durable decisions — L2

- Quality gates are authoritative. Mechanically green output is insufficient when semantics, emphasis, notation, provenance or reproducibility fail. Release promotion requires evidence from the real Product path and the full acceptance corpus.
- **Run59 remains the sole legal forward parent.** Canonical workflow `34753122528`, artifact `10316657472`, 3335 translation rows, exact source coverage and clean SQLite/FKs. Canonical DB SHA `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`; output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`. Current hard counts are **15 numeric / 10 punctuation / 0 length, 24 unique**.
- M2M100 arithmetic restatement remains accepted **default-OFF** with its existing pinned runtime/reproducibility contract. Historical Run20 workflow is restored to historical blob `a12988246a6306d3c6c9d0afdbf8f51bbbc0d983`.
- For seq3009, exact-row TC-big remains rejected as a Product direction because its raw output leaves untranslated English `fix'd`. Do not promote merely because punctuation becomes mechanically exact.
- The simple two-row forward-boundary trigger is superseded as the primary research geometry: it incorrectly required the unchanged left base row to be strict-research clean, so workflow `34755358217` found no cohort. Base routing may require only the defects relevant to discovering a rescue; **candidate acceptance must still require the complete maintained strict gates**. Do not weaken candidate selectors to compensate for dirty base output.
- The current source-defined geometry is the complete complementary-question Stage10 context. Workflow `34756122766` proved a generic cohort of exactly one context without corpus-position or lexical-anchor whitelists and a clause-first source partition with every chunk <=64 NLP tokens. The immutable source is translated only as raw rank0 chunks; source coverage is exact and database is read-only.
- Baseline OPUS clause-first output is hard-clean and restores the missing question, but is rejected because it introduces two quote delimiters around `Butter of Antimony`. The output-artifact gate is working as intended and must not be relaxed. Evidence SHA `8d959993fe5872d2c83e9a5404052724aa01eb95bbc9ac3c576f1de33e3266d8`.
- A pinned TC-big DOE over **the same discovery, segmentation and selector contract** is the only currently justified follow-up. Changing backend while holding geometry/gates constant is preferable to adding corpus-specific repairs. If TC-big also fails strict or semantic review, close this family and move to another residual class.
- Any accepted follow-up still requires a separate default-OFF production wrapper, full authenticated run59-based promotion and independent residual census before parent replacement.
- Promotion/diagnostic workflows must retain/upload rejection evidence before failing; orchestration failures are not model-negative evidence.

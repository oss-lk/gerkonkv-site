# RocketDict durable decisions — L2

- Quality gates are authoritative. A mechanically green result is insufficient when semantic, emphasis, notation, provenance, or reproducibility evidence fails. Release promotion requires evidence from the real Product path; gates may not be weakened to make a heavy run pass.
- **Authenticated run59 is the sole legal forward parent.** Canonical workflow `34753122528` + artifact `10316657472` verified exact run58 ancestry, run id 59, 3335 translation rows, only target seq2739 changed, byte-exact source coverage, SQLite integrity/FKs, raw rank0 provenance and all safety flags false. Canonical DB SHA `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`; output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`; target SHA `e7b3ee2688816ca5117326f1ca378d166b11910bcd364930d8e5ac645d5fed1e`; parameters SHA `5b1ea478963221869b64ebca2c9a60f8e9838c41e4616b9b737aa648436b4878`; canonical evidence SHA `64367c5a34413b8a348c66d4b2a64d2a3b70816d3fbb46eab6fc9b49d6732fea`; census SHA `8b99622628e62114c44ec3d8002797e42fd0658ee2d750288bafb7c6df8894ce`. Current hard counts are **15 numeric / 10 punctuation / 0 length, 24 unique**.
- M2M100 arithmetic restatement is an accepted **default-OFF** source-verifiable rescue class, not a replacement for the Product OPUS baseline. Trigger/selector remain generic: one `A x B (that is, above C)` restatement with integer `A*B=C`, exact immutable source, current intended numeric corruption, and candidate preserving exact arithmetic order/operator plus current strict hard/emphasis/volume rules. No corpus-position whitelist is allowed.
- M2M100 runtime contract is pinned to `facebook/m2m100_418M` revision `55c2e61bbf05dfb8d7abccdc3fae6fc8512fd636`, source-weight SHA `d907ea45e4e4b9db163382a6674f6218b3c59566fe06d77f4055c208b4e87ed1`, exact CTranslate2 `4.8.2`, CPU float32 and torch-free inference. Arithmetic wrapper contract is `/2`; public `rocketdict.api.operations.run_stage12` reaches it only under explicit opt-in. Beam 6, raw rank0-only, max decoding 512 remain the accepted generation contract.
- Public-Stage12 replay workflow `34752772395` is the decisive arithmetic reproducibility authority: three independently persisted run59 replicas all accepted exactly source start `483234`, changed only seq2739, and produced identical output/target-token identities. Aggregate evidence SHA `3203f6dd27be33e0812d147e13e8379e1a24c95a3b622db9a774a2599e4f4a37` authorized canonical verification. The subsequent no-inference canonical verifier and independent census, not the replay aggregate by itself, authorized parent replacement.
- Do **not** change max decoding length 512→384 or force MKL CNR merely for reproducibility. Linux AMD/Intel and two Windows Server 2025 probes reproduce the same arithmetic raw target/token identity under tested 384/512 and CNR variants. Converted CT2 asset bytes are platform/host-class sensitive; source-checkpoint identity and converted-asset identity are distinct. A release may distribute a canonical verified asset or explicitly platform-specific verified assets, but must not assume universal converter byte identity.
- Promotion/diagnostic orchestration must retain and upload raw rejected target/tokens, CTranslate2 version, generation parameters, asset manifest/tree identity, execution policy and host/platform evidence even when a selector rejects or a verifier fails. Stage12 fail-closed behavior is correct; runner-local evidence that disappears before upload is not enough to alter model/selector contracts.
- TC-big numeric-row routing remains default-OFF and may select only generic source-defined exact-row candidates with unique raw rank0 and current strict/emphasis/notation checks. Progression duplicate remains fail-closed; do not relax `_viz._` or `&c.` preservation. A numeric hard-gate PASS alone never authorizes a model route; historical M2M100 `десятьсот тысяч` stays semantically rejected.
- The next punctuation family must remain separate from arithmetic: source-defined internal leading-question paragraph-boundary truncation, still present in run59 seq3009/source start `531604`. Candidate implementation should be a narrow default-OFF exact-row TC-big wrapper if a run59 read-only trigger census proves a generic trigger isolates the intended geometry. It must use exact source and one raw rank0, preserve all existing hard/emphasis/artifact checks, and require a separate full-corpus promotion plus independent census before replacing run59.
- Historical M2M100 `_per deliquium_` remains closed because raw rank0 drops the emphasized phrase. Previously closed residual directions stay closed unless materially new L3 evidence appears.
- One-shot arithmetic/reproducibility/canonical workflows and temporary research harnesses are disposable after their conclusions are durably retained. Historical `.github/workflows/rocketdict-full-opticks-run20-m2m100-product-restatement-doe.yml` must be restored exactly from commit `6f5c8164f1d556aea55be6193cc3e864d9e0e09e`; historical workflow content is evidence and must not remain overwritten by temporary orchestration.

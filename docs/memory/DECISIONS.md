# RocketDict durable decisions — L2

- Quality gates remain authoritative; a mechanically green candidate is insufficient when semantic review finds loss of substantive content.
- Run56 remains the sole legal forward parent until corrected row-local parenthetical promotion is independently verified.
- Tested whole-context parenthetical replacement is not accepted because context `1977` semantically truncates despite mechanical eligibility.
- Preferred tested route is the default-OFF exact-row TC-big wrapper: generic source/target trigger, unique raw rank0, fail-closed selection; focused DOE `34744521997` accepted four of six trigger matches.
- Wrapper layering is part of run identity. The parenthetical wrapper must delegate through `translation_illustration_rescue_stage`; commit `6793fd19cc2c0e2a9779b72d15ad198624820cc0` fixes this.
- A new forward parent is allowed only after replay from exact run56 proves exactly four intended replacements, unchanged unrelated spans/targets, valid exact-source/raw-rank0 provenance, database integrity and independent hard counts **18 / 10 / 0, 27 unique**.
- Previously closed residual geometries stay closed unless materially new evidence appears.

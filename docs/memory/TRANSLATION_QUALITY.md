# RocketDict maintained translation quality — L2

- `rocketdict/PRODUCT_TARGET.md` and L3 evidence are authoritative; hard gates are unchanged and may not be weakened to make a corpus run pass.
- **Run58 remains the sole forward parent:** 3335 rows, **16 numeric / 10 punctuation / 0 length, 25 unique**. Promotion workflow `34748268143`, artifact `10315026413`; translation output SHA `60349256eac118e9af7aa1366761342cd2041bb6ade4db04b45c78b46a8c6e5d`; target SHA `8297e8b6cf20233284042549132bfc2eb4be312046964eab22cd7aa785eee3fd`; SQLite SHA `a24f35d6f8bb4747a4fc301511f383b8b866846a3c448655fce1b9359cc0ac94`; parameters SHA `1cf60c7b582640c3923ea685ed329eb685a7b80e502e11507d99c752e6342315`.
- Run58 promotion evidence SHA `0d5e893f5338e3b40c82a0e6eeb91d6f6b56dfc15fbae5974f0f6e9f042bb8c2`; independent census SHA `f4db643126c611e5f41e07af9d2ffa63dc43e9e9e48ea7884b0da7563ef9a803`. Exact run57 base, byte-exact source coverage, SQLite integrity/FKs, exact model-input/raw-rank0 provenance and 3333 unchanged targets were verified.
- Default-OFF TC-big numeric-row routing remains promotion-safe only for its already-proven source-defined compact numeric families. Progression duplicate is a durable negative because rank0 loses `_viz._`/rewrites `&c.`; those preservation gates stay strict.
- Pinned M2M100 `facebook/m2m100_418M` revision `55c2e61bbf05dfb8d7abccdc3fae6fc8512fd636`, weight SHA `d907ea45e4e4b9db163382a6674f6218b3c59566fe06d77f4055c208b4e87ed1`, has a verified immutable Product asset/runtime path: exact snapshot hashes -> float32 CTranslate2 asset -> CPU torch-free inference. Product default translation does not use it unless an explicit rescue layer is enabled.
- Real CT2 parity workflow `34748655508`, artifact `10315206479` (`sha256:3f90caf7dcf13a807da14a5d3ebb65100ee7ed9c6478d43b8853ff64d4912a1e`) proved source start `483234` raw CT2 rank0 byte-identical to historical PyTorch rank0 under `beam_size=6`, `num_hypotheses=1`, `max_decoding_length=384`; it preserves source-verifiable `700000 × 700000 = 490000000000`, passes strict mechanical/emphasis checks, evidence SHA `bc739df0594e5f73331c2a2d5301bdb6d6eb27d9ab3921703dc99a16237dac57`.
- Generic source-verified arithmetic trigger/selector and default-OFF Stage12 wrapper now exist. Trigger remains corpus-position-free: exactly one integer `A x B (that is, above C)` with `A*B=C`, intended current numeric corruption, otherwise clean hard structure; candidate must be exact immutable source -> unique raw rank0 and pass current strict/emphasis/volume/arithmetic-order/operator requirements. Public Stage12 now reaches this outer wrapper, but it stays disabled by default.
- Maintained CI after integration is green: `34749438950` (fixture correction), `34749689194` (public outer binding; dependency-light + real Stage8→25/unified smoke), and `34750071691` at diagnostic HEAD all passed. These runs prove default-OFF integration, not corpus promotion.
- **Arithmetic promotion reproducibility is unresolved.** Full replay `34749776816` authenticated exact run58 DB and both pinned TC-big/M2M100 assets, cache-resolved exact base run58, found exactly one arithmetic attempt at source start `483234`, but produced `accepted=0/rejected=1`, so no run59 was authorized. Diagnostic replay `34750071679`, with the same production wrapper/rules and authenticated base, instead produced `attempt=1, accepted=1, rejected=0`; it intentionally failed after printing diagnostics. This disagreement must be treated as possible raw-rank0/runtime nondeterminism until disproven.
- One known generation-contract mismatch exists: production arithmetic wrapper currently asks `max_decoding_length=512`, while the parity DOE that proved exact CT2/PyTorch parity used `384`. Do not change it on suspicion alone; diagnose whether raw target/verdict varies across repeated identical inference first. If generation contract is the cause, align to the already-proven value rather than weakening any selector gate.
- Potential next punctuation family is source-defined internal leading-question paragraph-boundary truncation: `?\n\n` inside the row with short prefix/long suffix and source one `?` vs target zero. On run58 this isolates seq3009/source start `531604`; historical exact-row TC-big rank0 passed hard punctuation, emphasis and volume. Keep it research-only until the arithmetic reproducibility boundary is closed.
- Historical M2M100 compound-million output `десятьсот тысяч` remains semantically rejected despite a green numeric gate. M2M100 is also closed for the `_per deliquium_` critical-emphasis residual because the historical rank0 drops that emphasized phrase.
- Previously closed residual geometries remain closed unless materially new evidence appears: seq638 short-DMS, seq325 figure-reference, historical angle-list/big-integer, inline `[G]`, broad fallback, automatic n-best, CT2 progression row, semantic-bad compound-million, and M2M100 `_per deliquium_`.

# RocketDict maintained translation quality — L2

- `rocketdict/PRODUCT_TARGET.md` and L3 evidence are authoritative; hard gates are unchanged and may not be weakened to make a corpus run pass. Reproducible model/runtime behavior is part of acceptance quality, not an optional optimization.
- **Run58 remains the sole legal forward parent:** 3335 rows, **16 numeric / 10 punctuation / 0 length, 25 unique**. Promotion workflow `34748268143`, artifact `10315026413`; translation output SHA `60349256eac118e9af7aa1366761342cd2041bb6ade4db04b45c78b46a8c6e5d`; target SHA `8297e8b6cf20233284042549132bfc2eb4be312046964eab22cd7aa785eee3fd`; SQLite SHA `a24f35d6f8bb4747a4fc301511f383b8b866846a3c448655fce1b9359cc0ac94`; parameters SHA `1cf60c7b582640c3923ea685ed329eb685a7b80e502e11507d99c752e6342315`. Promotion evidence SHA `0d5e893f5338e3b40c82a0e6eeb91d6f6b56dfc15fbae5974f0f6e9f042bb8c2`; independent census SHA `f4db643126c611e5f41e07af9d2ffa63dc43e9e9e48ea7884b0da7563ef9a803`.
- Default-OFF TC-big numeric-row routing remains promotion-safe only for already-proven source-defined compact numeric families. Progression duplicate is a durable negative because rank0 loses `_viz._`/rewrites `&c.`; those preservation gates stay strict.
- Pinned M2M100 `facebook/m2m100_418M` revision `55c2e61bbf05dfb8d7abccdc3fae6fc8512fd636`, weight SHA `d907ea45e4e4b9db163382a6674f6218b3c59566fe06d77f4055c208b4e87ed1`, remains an optional rescue/research component only. Exact snapshot -> float32 CTranslate2 asset -> CPU torch-free inference is required; Product default translation does not use it unless explicitly enabled.
- Real parity workflow `34748655508`, artifact `10315206479`, proved source start `483234` raw CT2 rank0 byte-identical to historical PyTorch rank0 under `beam=6`, `num_hypotheses=1`, `max_decoding_length=384`, preserving verified `700000 × 700000 = 490000000000`; parity evidence SHA `bc739df0594e5f73331c2a2d5301bdb6d6eb27d9ab3921703dc99a16237dac57`.
- Full replay `34749776816` produced one arithmetic attempt at `483234` but `accepted=0/rejected=1`; diagnostic replay `34750071679` on the same production code/run58 instead produced `attempt=1, accepted=1, rejected=0`. Because the first failed run did not persist the rejected raw rank0, its exact root cause cannot be reconstructed retrospectively. **Do not lower any selector threshold to resolve this.**
- The follow-up CTranslate2 determinism DOE materially narrows the risk. Workflow `34750592651` repeatedly translated the exact parity source with CTranslate2 `4.8.2`, two fresh processes × four repeats across current-auto/single-thread, `384/512`, and portable-DNNL configurations. AMD EPYC and Intel Xeon hosts all produced the same raw target SHA `258bc3a57d933b23317cf946af2f9e851fd266c6501a5b51813806b3294081d6`, raw-token SHA `599b77…`, exact parity, and selector PASS. The Intel replica verifier failed only because the research verifier incorrectly assumed one globally fixed CT2 asset-tree SHA; inference itself passed.
- Upstream CTranslate2 documents CPU backend/ISA auto-selection and has an open CPU reproducibility issue explaining that MKL/oneDNN numerical differences can change beam output. Focused forced-MKL CNR workflow `34751000550` therefore tested `CT2_USE_MKL=1`, `MKL_CBWR=COMPATIBLE`, fixed one thread/dynamic-off/AVX2/packed-GEMM-off. Three AMD generations, both `384` and `512`, multiple fresh processes all produced the same exact-parity target and selector PASS; cross-host CNR evidence SHA `a4d8a4d0…`. This is a proven fallback execution policy, not yet a reason to change Product runtime.
- CT2 asset conversion has its own portability dimension. Asset reproducibility workflow `34751186058` rebuilt the same pinned snapshot twice on each of three AMD runners; builds were byte-identical within host and across all three AMD hosts with payload tree `09d8b2e6b5941382aa7c695d2fbbf8918244a8df62ca9befc72d89e43f4642eb`, cross-host evidence SHA `b0ccd7758590a7761eb9a46b33bfdeb677524ac39b019431cf492152787b844b`. A separate Intel conversion from the broad DOE produced a different payload tree while still yielding the same raw rank0, proving that converter byte identity can vary by host class even when inference for this case does not.
- Windows reproducibility workflow `34751346680` is now the decisive release-portability probe. One completed Windows Server 2025 replica built a distinct Windows asset tree `3f3539411fb3eed88ebbf4c42b73f0f044b3b33e9182f46e0f897c57b195d7f5` yet every current/single/portable `384/512` raw rank0 and every forced-MKL-CNR `384/512` raw rank0 exactly matched parity target SHA `258bc3…` and selector PASS. Windows evidence SHA for that replica is `39e83e5bf846a2b72cbb4cd698ecb7513fc41ca9151232194ac4440f1da55252`. A second Windows replica was still running when this memory record was synchronized; do not claim cross-Windows completion until L3 confirms it.
- Current evidence therefore does **not** justify changing `max_decoding_length=512` to `384`: both values are exact-parity across the tested Linux and Windows standalone matrices. The earlier 384/512 mismatch remains a historical contract difference, but is no longer a supported root-cause hypothesis for the single rejected promotion.
- Future promotion evidence must persist rejected raw rank0 text/tokens plus CTranslate2 version, asset identity, generation parameters/execution policy, and host/platform identity. Fail-closed selection is correct, but a rejection without raw forensic provenance is insufficient for reproducibility diagnosis.
- Potential next punctuation family remains source-defined internal leading-question paragraph-boundary truncation (`?\n\n` inside one row, short prefix/long suffix, source one `?`, target zero), isolating run58 seq3009/source start `531604`; historical exact-row TC-big rank0 passed hard punctuation/emphasis/volume. Keep it research-only until arithmetic promotion is closed.
- Historical M2M100 compound-million output `десятьсот тысяч` remains semantically rejected despite a green numeric gate. M2M100 is also closed for `_per deliquium_` because historical rank0 drops the emphasized phrase. Previously closed residual geometries remain closed unless materially new evidence appears.

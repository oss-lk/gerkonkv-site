# RocketDict maintained translation quality — L2

- `rocketdict/PRODUCT_TARGET.md` and L3 evidence are authoritative. Hard gates must not be weakened merely to make a corpus pass. Persisted rescue output remains exact source + unique raw rank0 only; no target surgery, literal injection, placeholders, source rewriting, corpus-position patches or automatic n-best cherry-picking.
- **Run59 is the sole legal forward parent.** Canonical workflow `34753122528`, artifact `10316657472`, 3335 rows, hard counts **15 numeric/symbol / 10 punctuation / 0 length, 24 unique**. Output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`; canonical SQLite SHA `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`; canonical evidence SHA `64367c5a34413b8a348c66d4b2a64d2a3b70816d3fbb46eab6fc9b49d6732fea`; independent census SHA `8b99622628e62114c44ec3d8002797e42fd0658ee2d750288bafb7c6df8894ce`.
- M2M100 arithmetic restatement remains an accepted **default-OFF** source-verifiable rescue class. Its pinned runtime/reproducibility contract remains unchanged.
- Current punctuation frontier seq3009 is a planner/model interaction inside Stage10 context2730. Run59 member rows are 3005..3009; row3009 starts with the trailing question `fix'd as Lead?` then a blank paragraph and new material. Historical exact-row TC-big restores punctuation but leaves untranslated `fix'd`, so exact-row fallback remains insufficient.
- Two-row forward-boundary DOE v2 workflow `34755358217` authenticated the correct run59 DB and OPUS asset but discovered zero eligible pairs. Root cause is routing, not model evidence: row3008 is Product-hard clean but its current target introduces two quote delimiters around `Butter of Antimony`, so requiring the unchanged base-left row to be strict-research clean suppresses the intended geometry. This base-only routing condition must not be confused with candidate acceptance; candidates remain subject to every strict gate.
- The stronger source-defined complementary-question clause DOE is successful and is the current evidence authority: workflow `34756122766`, artifact `10317896538`, digest `sha256:7aaec9e01bf76a6e67a90dfab17bd104be4cfa7303771e58af6c42b806ea2dc0`, evidence SHA `8d959993fe5872d2c83e9a5404052724aa01eb95bbc9ac3c576f1de33e3266d8`. It discovers exactly context2730 from complementary punctuation-failure geometry, then partitions the immutable source into clause-first chunks `[42,52,30,38,54,44,56]` NLP tokens with punctuation priority `;`, then `,`, no sequence/source-offset whitelist and exact source coverage.
- OPUS raw rank0 for that clause-first geometry restores the missing question and the aggregate passes numeric/symbol, punctuation, length, emphasis, numeric order, delimiters and critical-token checks with non-decreasing alphabetic volume. It is **not promotable** because output-artifact diagnostics detect two target-only quote delimiters around the translation of `Butter of Antimony`; aggregate `strict_research_passed=false` solely on that surface.
- HEAD contains a read-only TC-big raw-rank0 DOE using the exact same generic discovery/segmentation and unchanged selectors. This is the next bounded experiment. If it remains strict-clean and semantically acceptable it may justify a separate default-OFF wrapper; otherwise close the direction without weakening quote/artifact gates.
- Promotion/diagnostic orchestration must upload raw rejected targets/tokens and runtime/asset/generation evidence before failing. Orchestration identity failures are not model-negative evidence.

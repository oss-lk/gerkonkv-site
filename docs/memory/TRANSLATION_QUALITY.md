# RocketDict maintained translation quality — L2

- `rocketdict/PRODUCT_TARGET.md` and L3 evidence are authoritative. Hard gates must not be weakened merely to make a corpus pass. Persisted rescue output remains exact immutable source plus unique raw rank0 lexical output; source-owned structural cells may be rendered from a source plan created before MT, as already established for table/illustration structure. No post-MT target repair, literal injection, placeholders, source rewriting, corpus-position patches or automatic n-best cherry-picking.
- **Run59 is the sole legal forward parent.** Canonical workflow `34753122528`, artifact `10316657472`, 3335 rows, hard counts **15 numeric/symbol / 10 punctuation / 0 length, 24 unique**. Output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`; canonical SQLite SHA `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`; canonical evidence SHA `64367c5a34413b8a348c66d4b2a64d2a3b70816d3fbb46eab6fc9b49d6732fea`; independent census SHA `8b99622628e62114c44ec3d8002797e42fd0658ee2d750288bafb7c6df8894ce`.
- Seq3009 remains unresolved. OPUS complementary-question clause DOE `34756122766` restored the question but was rejected because it introduced two target-only quote delimiters. TC-big follow-up `34756713385` failed before inference because its isolated venv omitted dependencies required by the existing TC-big runtime status check; it is not model-negative evidence. Quote/artifact gates remain strict.
- The combined-Greek residual is a stronger bounded direction. Run59 contains exactly two combined-Greek technical atoms: seq282/source 46865 preserves `_nvt[Greek: t]_`, while seq1753/source 301051 loses `_A[Greek: a]_`. Generic source/content trigger isolates exactly the lost row without sequence/source-start whitelist.
- Read-only source-plan DOE `34757359712` succeeded. Artifact `10317379028`, digest `sha256:edca7a983d859c3b25f3caa4678c7fc297eab6b2a16e8153d06ab1d9b76a4b97`, evidence SHA `29ae03c4c5609ca698a578ca7b876e5111fd4d799fe74f1a7584099097e93b73`. It creates a pre-MT plan `prefix + separator + _A[Greek: a]_ + separator + suffix`, keeps the technical atom/separators source-owned, and sends only exact prefix/suffix lexical spans to OPUS beam6/rank0/maxlen512. Prefix rank0 is `Тогда разделите линию`; suffix rank0 preserves all required numeric literals and `&c.`. The assembled semantic carrier is strict-clean under numeric/symbol, punctuation, length, numeric-order, delimiter, critical-token, output-artifact and emphasis checks and has non-decreasing alphabetic volume. Source coverage is byte-exact and DB is unchanged.
- The default-OFF wrapper `rocketdict-stage12-combined-greek-source-plan-rescue/1` implements that same generic plan outside the run59 arithmetic layer. It requires exactly one combined technical atom with lexical material on both sides, a lost combined atom in the base target, and current hard/critical-token failure; rejected candidates preserve the base row exactly. Stage-only maintained CI `34757474185` was fully green.
- HEAD test run `34757502767` failed only because a monkeypatched `_complete` in the new unit test returns the stored `items` list instead of the output dict. Product Core otherwise had 415 passes and real-runtime succeeded. This is a fixture/orchestration defect; production selector/gates must not change to fix it.
- Promotion/diagnostic orchestration must upload raw rejected targets/tokens and runtime/asset/generation evidence before failing. Orchestration identity/dependency failures are not model-negative evidence.

# RocketDict maintained translation quality — L2

- `rocketdict/PRODUCT_TARGET.md` and L3 evidence are authoritative. Hard gates must not be weakened merely to make a corpus pass. Persisted rescue output remains exact source + unique raw rank0 only; no target surgery, literal injection, placeholders, source rewriting, corpus-position patches or automatic n-best cherry-picking.
- **Run59 is the sole legal forward parent.** Canonical workflow `34753122528`, artifact `10316657472`, 3335 rows, hard counts **15 numeric/symbol / 10 punctuation / 0 length, 24 unique**. Output SHA `5feeb168b77c3a763ab01fd5c9c0c25aa02c62d67899f8b1358414099735fbb1`; canonical SQLite SHA `41bafa00619c83b87f55f7e452f8e9e27a1871d601bb5c846d3d561531dbb78d`; canonical evidence SHA `64367c5a34413b8a348c66d4b2a64d2a3b70816d3fbb46eab6fc9b49d6732fea`; independent census SHA `8b99622628e62114c44ec3d8002797e42fd0658ee2d750288bafb7c6df8894ce`. Only seq2739 differs from run58 and parent replacement is verified.
- M2M100 arithmetic restatement is accepted as a **default-OFF** source-verifiable rescue class. Three authenticated public-Stage12 replicas reproduced the same accepted seq2739 raw rank0 and semantic output. Linux/Windows reproducibility is sufficient to keep the pinned runtime contract; do not switch max decoding 512→384 or force MKL CNR without new evidence.
- Promotion/diagnostic orchestration must upload raw rejected targets/tokens and runtime/asset/generation evidence before failing. Orchestration identity failures are not model-negative evidence.
- Current punctuation frontier is seq3009. L3 shows its source begins with a short trailing question fragment `fix'd as Lead?` and then a blank paragraph boundary plus a long new paragraph. Planner-v8 cuts the preceding row at its 64-token soft budget, leaving this 4-token tail at the beginning of the next row, where OPUS omits it. This is better treated first as a source/planner boundary-resegmentation problem than as a second-model fallback.
- Historical exact-row TC-big rank0 for seq3009 mechanically restores the missing question mark but leaves untranslated English `fix'd`; therefore it is not sufficient semantic-quality evidence for promotion.
- A read-only baseline-OPUS forward paragraph-boundary resegmentation DOE now exists. The generic trigger is source/planner-defined rather than sequence-whitelisted: same split Stage10 context, left row at soft budget, right row starts with a short `...?` fragment followed by a blank paragraph boundary and substantial suffix, right row otherwise clean except question deficit, and only a bounded forward token move. Initial workflow `34755006902` failed before inference because of a stale run59 SQLite identity. The correct canonical DB SHA is the one above; corrected launcher `real_translation_run59_forward_paragraph_boundary_resegmentation_doe_v2.py` changes only that immutable identity and awaits successful replay.
- If the corrected DOE does not produce two strict-clean, emphasis-preserving, semantically sound raw OPUS rank0 rows and a strict-clean aggregate without alphabetic loss, close this direction rather than relaxing gates. If it passes, promote only through a separate default-OFF wrapper, full run59-based replay and independent residual census.

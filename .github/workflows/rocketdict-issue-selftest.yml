name: RocketDict issue Actions self-test
on:
  issues:
    types: [opened]
jobs:
  selftest:
    if: startsWith(github.event.issue.title, 'RocketDict Actions self-test')
    runs-on: ubuntu-latest
    steps:
      - run: echo ROCKETDICT_ISSUE_ACTIONS_OK

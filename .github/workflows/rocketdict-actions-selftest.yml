name: RocketDict Actions self-test
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/rocketdict-actions-selftest.yml'
jobs:
  selftest:
    runs-on: ubuntu-latest
    steps:
      - name: Self-test
        run: echo ROCKETDICT_ACTIONS_OK

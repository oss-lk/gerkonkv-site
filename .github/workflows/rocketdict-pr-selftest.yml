name: RocketDict PR Actions self-test
on:
  pull_request:
    branches: [main]
    paths:
      - 'rocketdict-ci/**'
jobs:
  selftest:
    runs-on: ubuntu-latest
    steps:
      - run: echo ROCKETDICT_PR_ACTIONS_OK

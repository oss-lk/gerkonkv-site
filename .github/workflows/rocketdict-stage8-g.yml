name: RocketDict Stage 8 G n-best probe

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/rocketdict-stage8-g.yml'
      - 'rocketdict-ci/stage8_g_nbest_probe.py'
      - 'rocketdict-ci/stage8-g-trigger.txt'
  workflow_dispatch:

jobs:
  stage8-g-nbest-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install real OPUS runtime
        run: |
          python -m pip install --upgrade pip
          python -m pip install 'ctranslate2>=4.5,<5' 'sentencepiece>=0.2,<1' 'PyYAML>=6,<7'
      - name: Run Stage 8 G n-best integrity mechanism probe
        run: python rocketdict-ci/stage8_g_nbest_probe.py
      - name: Upload Stage 8 G evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rocketdict-stage8-g-nbest-probe
          path: work-stage8-g/evidence/**
          if-no-files-found: warn
          retention-days: 14
